export enum ReservationStatus {
  PENDING = 'PENDING',
  PAYMENT = 'PAYMENT',
  CONFIRMED = 'CONFIRMED',
  REJECTED = 'REJECTED',
  DONE = 'DONE',
}

export enum TimeSlot {
  MORNING = 'MORNING',
  AFTERNOON = 'AFTERNOON',
  FULLDAY = 'FULLDAY',
}
